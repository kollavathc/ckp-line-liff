{
  "name": "apps-script",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && esbuild src/index.ts --bundle --platform=browser --target=es2020 --format=iife --global-name=App --outfile=dist/Code.js --footer:js=\"globalThis.doGet=App.doGet;globalThis.doPost=App.doPost;\" && cp appsscript.json dist/appsscript.json",
    "deploy": "pnpm build && clasp push",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "@ckpharmacy/shared": "workspace:*"
  },
  "devDependencies": {
    "@google/clasp": "^3.0.6-alpha",
    "@types/google-apps-script": "^1.0.99",
    "@types/jest": "^29.5.14",
    "esbuild": "^0.25.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.6"
  }
}
