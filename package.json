{
  "name": "ckpharmacy-line-liff",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.34.1",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "dev": "pnpm --filter frontend dev",
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present test"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}

